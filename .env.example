# Local secrets only — copy to `.env` (gitignored). Never commit real tokens.
#
# Azure DevOps PAT with Marketplace → Manage, organization = All accessible orgs.
# The Microsoft identity behind the PAT must be an Owner of the Marketplace publisher.
VSCE_PAT=

# Optional: Open VSX personal access token
# OVSX_PAT=
